can you plese suggest me a better project so that i must increase the chance of winning and that project must be paarctical and market need and must be eye catching...???

22:33
Claude responded: Good instinct.
Good instinct. Let me be honest with you: SwarmChain was a "smart-sounding" project, not a winning one. Federated learning on Solana has been pitched in every Solana hackathon since 2023, none have won, and judges glaze over the moment they hear "decentralized AI training." It's a graveyard category.

Let me give you the brutally honest filter for what actually wins, then 5 ideas, then my pick.

What actually wins Colosseum hackathons
I looked at recent winners and the pattern is consistent. Winners are boring-looking products solving expensive problems for real people — a hardware wallet, a prediction market aggregator, a remittance app, a buy-now-pay-later, an oracle for real-world assets. Not one was "AI + blockchain + decentralized X." Judges (most are VCs and operators) ask three questions in their head:

Is the market obvious and big? (Can I name 3 customers in 10 seconds?)
Does Solana actually matter here? (Or could this be a Postgres database?)
Could this be a real company in 6 months? (Not "in 5 years if we figure out X.")
Anything that needs a paragraph to explain why someone would use it is dead on arrival.

Five projects that pass that filter, ranked
1. RemitSol — Stablecoin remittance from Gulf to Pakistan/India, built mobile-first You're in Pakistan. You have a massive unfair advantage here. Pakistan received ~$30B in remittances in 2024, mostly from UAE/Saudi, and people pay 5–7% in fees through Western Union and Wise. Solana settles in 400ms for $0.0001. Build a mobile PWA: sender in Dubai loads USDC via card → recipient in Karachi withdraws PKR via JazzCash/Easypaisa off-ramp. You don't need to actually integrate with a real off-ramp — partner with a mock one or simulate the cash-out with a QR code. The pitch writes itself: "I'm from Pakistan. My family receives remittances. Here's what it costs them today vs. what it costs on Solana." A previous Solana hackathon winner was literally "Stablecoin rails for remittance to India" — this category demonstrably wins. 
Superteam

2. PayrollSol — USDC payroll for crypto-native companies paying global contractors Deel charges 3–8% on contractor payments and takes 3–5 days. Build a dashboard where a company connects their Solana wallet, uploads a CSV of contractors (name, country, USDC wallet, monthly amount), schedules recurring payments, and gets on-chain proof of payment for accounting. Add tax form generation as a stretch goal. Boring, ugly, lucrative — exactly what wins. Real customers exist today, every crypto startup needs this.

3. ProofOfReceipt — On-chain warranty + receipt protocol for e-commerce Every Shopify store emails you a receipt that gets buried in Gmail. When you need a warranty claim 8 months later, you can't find it. Build a Shopify app that mints a compressed NFT receipt to the buyer's wallet at checkout — proof of purchase, warranty terms, serial number, all on-chain. Buyer scans QR at the store for warranty service. This is the cNFT killer use case — millions of receipts at fractions of a cent each, only economically viable on Solana. Massive TAM, dead-obvious utility, beautiful demo.

4. SplitSol — Splitwise but it actually settles Splitwise tracks who owes what but you still have to Venmo each other. Build a group expense app where roommates/travel groups add expenses in any currency, the app tracks balances, and at month-end one click settles everyone via USDC on Solana. Solves the "I'll pay you back later" problem permanently. Massive consumer appeal, instantly demoable, the kind of thing a non-crypto person would actually use.

5. AuditTrail — Compliance-ready transaction labeling for crypto businesses (the AI+Solana angle that's actually defensible) Crypto companies struggle with accounting because their wallets emit thousands of cryptic transactions. Build a tool where they connect their Solana wallets, an AI labels every transaction (revenue, expense, transfer, fee, swap), and exports to QuickBooks/Xero format. This is the only AI angle that wins because the AI is the boring backend, not the pitch — the pitch is "we save crypto CFOs 40 hours a month."

My pick: RemitSol
I'd build RemitSol, and it's not close. Here's why:

You have lived experience. You're in Sahiwal, Pakistan. You can stand on camera and say "my country received $30B last year and lost $1.5B in fees to companies that take 5 days. Watch me do it in 4 seconds for free." That story beats every other team in your bracket.
Solana is genuinely the right answer. Remittance is the textbook killer app for stablecoins on a fast chain. Judges already believe this.
Buildable in 6 days. A mobile-first PWA, a Solana program for escrow, a USDC integration on devnet, a fake off-ramp UI. No federated learning. No ZK proofs. No swarm anything.
Demo is unforgettable. Split screen: Western Union showing $25 fee + 3 days vs. your app showing $0.0001 + 4 seconds. Done. That's the whole pitch.
It maps to a track Colosseum loves. Payments and stablecoins are the most-funded category in every recent Solana hackathon — winners include "Stablecoin rails for remittance to India", and stablecoin/payments projects dominate the recent Hall of Fame. 
Superteam
What you do in the next 24 hours if you pick this
Drop the SwarmChain repo. Spin up remitsol/ with: Next.js PWA (mobile-first, Tailwind, shadcn), @solana/wallet-adapter + USDC on devnet, an Anchor program with two instructions (create_transfer, claim_transfer with a recipient wallet or claim code), Phantom mobile deeplink for sender, a fake JazzCash/Easypaisa cash-out screen for recipient. Build the sender flow first end-to-end before touching the recipient side. Record demo on actual phones, not a desktop simulator — the mobile-on-mobile demo is the visual that sells it.

One last brutally honest thing: pick in the next hour and commit. You've already burned 1 of your 6 days. The teams that win are not the ones with the best idea — they're the ones who picked a decent idea on day 1 and shipped relentlessly. RemitSol, PayrollSol, SplitSol — any of these beats SwarmChain. Pick one, close this chat, and start.

Want me to draft the RemitSol architecture, the Anchor program skeleton, or the pitch script?

You said: will it be free to make?
will it be free to make??? if yes...then do so...give me full best possible stack and i have opus 4.7 so i will definately make this done

22:40
Claude responded: Yes, 100% free to build.
Yes, 100% free to build. Every component below has a free tier that more than covers a hackathon demo. No credit card required for any of them. Let me lay out the full winning stack, then we'll go.

Free-tier breakdown (so you know nothing surprises you)
Solana devnet — completely free, unlimited. solana airdrop 2 gives you free SOL whenever you need it. USDC has a devnet faucet too (Circle's devnet faucet, free).

Helius RPC free tier — 1M credits/month, WebSocket support, more than you'll touch.

Vercel free tier — unlimited deploys, custom domain, automatic HTTPS. Hosts your Next.js PWA forever for free.

GitHub — free public repo, free Actions minutes, free Pages if you ever want them.

Pinata IPFS free tier — 1GB storage, plenty for any metadata/images you need.

Phantom & Solflare wallets — free, no API keys needed.

Anchor framework — open source, free.

All the npm packages, Rust crates, Python libs — free, MIT/Apache licensed.

Domain (optional) — skip it. Use the free remitsol.vercel.app subdomain. Nobody cares for a hackathon.

Logo + branding — free via any AI image tool. 20 minutes of work.

Demo video hosting — free on YouTube unlisted, or Loom free tier (5 min limit, perfect for you).

Total spend: $0.00. You can build this on a $300 laptop with stable internet.

The winning stack — RemitSol
On-chain layer
Solana devnet as the network
Anchor 0.31+ (Rust) for the program — one program, three instructions: create_transfer, claim_transfer, cancel_transfer
SPL Token for USDC integration (devnet USDC mint: 4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU)
Program Derived Addresses (PDAs) to escrow funds — sender locks USDC into a PDA, recipient claims with a 6-digit code or wallet address
Frontend layer
Next.js 15 (App Router) + TypeScript
Tailwind CSS + shadcn/ui for components
Framer Motion for the wow animations (money flying across a map, etc.)
PWA-first via next-pwa — installable on iPhone/Android home screens, looks like a native app
react-qr-code for the claim-code QR
Recharts for the "fees saved" comparison graph
lucide-react for icons
Sonner for toast notifications
Solana integration
@solana/wallet-adapter-react + @solana/wallet-adapter-react-ui
@solana/wallet-adapter-wallets (Phantom, Solflare, Backpack)
@solana/web3.js v1 (not v2 — too new, sparse docs)
@coral-xyz/anchor for typed program client
@solana/spl-token for USDC transfers
bs58 for keypair handling
Mobile experience
Phantom Mobile deeplinks for sender flow on phone (https://phantom.app/ul/v1/...)
WhatsApp share API for sending the claim code to recipient (huge in Pakistan — every recipient has WhatsApp)
Web Share API as fallback
Polish layer (this is what sells it)
A live "fee comparison" widget — animated counter showing "$24.50 saved vs Western Union" updating per transfer
A leaderboard of "money saved" — total community fees avoided since launch
Real testimonials mocked from your own family/friends in PK (with permission) — adds humanity
Dev tooling
pnpm workspace (monorepo: program + web)
Turbo for parallel builds (free, optional)
Anchor's built-in test framework (mocha) for one happy-path test
GitHub Actions for "anchor build" + "next build" on every push — looks professional to judges browsing your repo
What you intentionally skip
❌ Real fiat off-ramp integration (you'll mock JazzCash/Easypaisa with a beautiful fake screen — judges understand)
❌ KYC (mention as roadmap, don't build)
❌ Multi-currency (USDC only, mention PKR/INR/PHP as roadmap)
❌ Backend server (everything is on-chain or in the browser — no server = free hosting + faster dev)
❌ Database (your "database" is the Solana program state)
❌ Auth (wallet IS the auth)
This last list is critical. Every "we won't build X" is a day saved. Your scope discipline is what gets you to the finish line.

Folder structure
remitsol/
├── programs/
│   └── remitsol/
│       ├── src/
│       │   ├── lib.rs
│       │   ├── instructions/
│       │   │   ├── create_transfer.rs
│       │   │   ├── claim_transfer.rs
│       │   │   └── cancel_transfer.rs
│       │   ├── state/
│       │   │   └── transfer.rs
│       │   └── errors.rs
│       └── Cargo.toml
├── apps/
│   └── web/
│       ├── app/
│       │   ├── layout.tsx
│       │   ├── page.tsx              # Landing — the pitch
│       │   ├── send/page.tsx         # Sender flow
│       │   ├── claim/[code]/page.tsx # Recipient flow
│       │   └── stats/page.tsx        # Live "fees saved" leaderboard
│       ├── components/
│       │   ├── wallet/
│       │   ├── send-flow/
│       │   ├── claim-flow/
│       │   ├── fee-comparison/       # The killer animation
│       │   └── ui/                   # shadcn
│       ├── lib/
│       │   ├── anchor-client.ts
│       │   ├── usdc.ts
│       │   ├── claim-code.ts         # 6-digit code generator
│       │   └── constants.ts
│       ├── hooks/
│       │   ├── use-transfer.ts
│       │   └── use-balance.ts
│       ├── public/
│       │   ├── manifest.json         # PWA manifest
│       │   └── icons/
│       ├── next.config.mjs
│       ├── tailwind.config.ts
│       └── package.json
├── tests/
│   └── remitsol.ts                   # one happy-path mocha test
├── scripts/
│   ├── deploy-devnet.sh
│   └── airdrop-usdc.ts
├── Anchor.toml
├── Cargo.toml
├── pnpm-workspace.yaml
├── package.json
├── .github/
│   └── workflows/
│       └── ci.yml
├── README.md                         # The most leveraged file
└── DEMO.md                           # 3-minute video script
The 6-day execution plan, hour-budgeted
Day 1 — today (Tue May 5) — Foundation, ~6 hours

Initialize repo, push to GitHub public from minute one
anchor init remitsol, write the three instructions, deploy to devnet
Initialize Next.js app, wire wallet-adapter, connect Phantom successfully
Goal: at end of day, you have a button that says "Connect Wallet" and shows your devnet USDC balance
Day 2 — Wed — Sender flow end-to-end, ~7 hours

create_transfer UI: amount input, generate 6-digit claim code, sign transaction, USDC moves to PDA
Show success screen with claim code + WhatsApp share button
Goal: you can send $10 USDC to a claim code from your laptop
Day 3 — Thu — Recipient flow + mobile polish, ~7 hours

/claim/[code] page: enter code, connect wallet, claim USDC from PDA
Mock JazzCash/Easypaisa "withdraw to mobile money" screen — beautiful fake that ends with "PKR 2,800 sent to +92xxx"
Test on actual phone (yours and a second one if possible)
Goal: full round trip from laptop sender to phone recipient works
Day 4 — Fri — The "wow" frontend, ~8 hours

Landing page with the fee-comparison animation (Western Union vs. RemitSol counter)
Stats page with mock leaderboard ("$X saved by Pakistani families this week")
PWA manifest + icons so it installs to home screen
Polish every screen — this is the day judges' eyeballs will land on
Goal: someone could mistake this for a real product
Day 5 — Sat — Demo + pitch, ~6 hours

Record 3-minute demo video on actual phones (split-screen sender/recipient)
Write README with architecture diagram + GIF + 1-command setup
Write the Colosseum submission text — fill every field on the form
Logo + 3 screenshots for the submission gallery
Goal: submission is 95% complete, just not pressed yet
Day 6 — Sun May 10 — Buffer + submit, ~4 hours

Test everything on a clean machine / clean Phantom
Fix the one ugliest thing
Submit Sunday night or Monday morning — never wait for Monday evening, the portal will be slammed
You have ~38 working hours across 6 days. This scope fits in 38 hours if you don't yak-shave.

What I need from you to start writing code
Confirm three things and I'll start producing the actual code in the next message:

You have Node.js 20+, Rust, Solana CLI, and Anchor installed? (If not, I'll give you the install commands first.)
You're on Mac/Linux/WSL, or native Windows? (Anchor on native Windows is painful — WSL is the answer.)
You want me to start with the Anchor program (on-chain logic) or the Next.js landing page (the visible pitch)? I recommend Anchor program first — everything else is downstream of it.
Once you tell me, I'll write the full lib.rs, the three instruction handlers, the test, and the deployment script in one go. Then we move to the frontend.

Pick a lane. Let's ship.

You said: i have node and rust but i have not solana cli and anchor.
i have node and rust but i have not solana cli and anchor...what if i use metamaks as i have developer account of it...and secondly...will it be deployable to web??? and i am using windows and i have no money yet to even transfer so what i have to do now??

23:35
Claude responded: Stop.
Stop. Three of your four questions reveal the same misconception, and I need to clear it up before you write any code, because if I don't you'll waste 2 days going down the wrong path.

The hard truths, one by one
1. MetaMask does not work on Solana. Period.

MetaMask is for EVM chains — Ethereum, Polygon, BSC, Base, Arbitrum, etc. Solana is a completely different blockchain with a different cryptography (Ed25519 vs ECDSA), different address format (base58 vs hex), different programming model (accounts vs storage slots), different VM (SVM vs EVM). Your MetaMask developer account is worth zero on Solana. There is a "MetaMask Snap" for Solana that exists, but no Colosseum judge will accept it as the wallet integration — they expect Phantom, Solflare, or Backpack.

You must install Phantom — it's free, takes 60 seconds, no account needed. Just a browser extension + a seed phrase you write down. Switch the network to "Devnet" in settings. Done.

If you build on Ethereum/Polygon to use MetaMask, you are automatically disqualified from Colosseum. The hackathon is Solana-only. I told you this in my first reply — I want to make sure it landed.

2. "Deployable to web" — yes, and it costs $0.

This is the easy part. Your Next.js frontend deploys to Vercel free tier: connect your GitHub repo, push to main, it auto-deploys to remitsol.vercel.app with HTTPS. Forever free. No credit card.

Your Solana program deploys to devnet, which is also free. Devnet is a fully functional public Solana network — anyone in the world can interact with your deployed program from their browser. It's not localhost. The only difference between devnet and mainnet is that devnet SOL has no real-world value.

So yes — anyone on the internet will be able to use your deployed app on a real phone with a real Phantom wallet on devnet. That's exactly what judges want.

3. "I have no money to even transfer" — this is also a non-issue, and I think you're confusing two things.

You don't need real money for any of this. Devnet has free faucets:

Free SOL: run solana airdrop 2 in your terminal, or visit faucet.solana.com — gives you 2 SOL instantly to your devnet wallet. You can do this multiple times. SOL on devnet has no real value, but it lets you pay devnet transaction fees (which are like $0.0001 anyway).
Free USDC on devnet: Circle runs a devnet faucet at faucet.circle.com — gives you free devnet USDC to test with. Or you can mint your own fake USDC token in 1 minute and call it "USDC" for the demo.
The transfers in your app will be devnet USDC, not real USDC. The judges know this. Every Colosseum project is on devnet. Nobody loses real money.

4. Windows — you need WSL2. Today.

Anchor on native Windows is genuinely broken. You will spend 2 days fighting build errors. WSL2 (Windows Subsystem for Linux) is free, ships with Windows 10/11, and gives you a real Ubuntu environment that just works. This is non-negotiable.

Your install path, in order, on Windows
Run these in this exact sequence. Each step has a verification command — don't move on until it passes.

Step 1: Install WSL2 (5 minutes)
Open PowerShell as Administrator and run:

wsl --install
Reboot. After reboot, Ubuntu opens automatically and asks you to set a Linux username + password. Pick anything, write it down.

From this point forward, do everything inside the Ubuntu/WSL terminal. Not PowerShell. Not CMD. WSL.

Step 2: Install build essentials inside WSL
sudo apt update && sudo apt upgrade -y
sudo apt install -y build-essential pkg-config libssl-dev libudev-dev
Step 3: Install Rust inside WSL (your existing Windows Rust doesn't count — WSL is a separate Linux env)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env
rustc --version
Step 4: Install Node.js inside WSL via nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
source ~/.bashrc
nvm install 20
nvm use 20
node --version
Step 5: Install Solana CLI
sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"
Then add it to your path (the installer tells you the exact line — usually):

export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
Add that line to your ~/.bashrc so it persists. Then verify:

solana --version
Step 6: Install Anchor via avm (Anchor Version Manager)
cargo install --git https://github.com/coral-xyz/anchor avm --force
avm install latest
avm use latest
anchor --version
This step takes 5–10 minutes — it's compiling. Let it run.

Step 7: Configure Solana for devnet + create your devnet wallet
solana config set --url https://api.devnet.solana.com
solana-keygen new
solana airdrop 2
solana balance
You should see 2 SOL. You now have free devnet money. This is the wallet you'll use to deploy your program.

Step 8: Install Phantom
Open Chrome on Windows (not WSL — Phantom is a browser extension)
Install Phantom from the Chrome Web Store
Create a new wallet, write down the seed phrase
Click the gear icon → Developer Settings → Testnet Mode: ON → select Devnet
Now your Phantom is on devnet
Step 9: Get devnet USDC for testing
Visit faucet.circle.com, paste your Phantom devnet address, request USDC
Or just use SOL for the v1 demo and add USDC on Day 2
Step 10: Verify everything by initializing a hello-world Anchor project
cd ~
mkdir dev && cd dev
anchor init hello
cd hello
anchor build
If anchor build succeeds, your environment is ready and you've eliminated 90% of the bugs that kill hackathon teams.

What you do next, today, in this exact order
Run steps 1 through 10 above. Budget 2-3 hours, including the 10-minute Anchor compile waits. Do this before anything else. If a step fails, paste the error to me and I'll debug it.
Once anchor build succeeds in the hello project, delete it. We'll start remitsol clean.
Reply to me with "environment ready" and I'll generate the full Anchor program for RemitSol — lib.rs, all three instruction handlers, the state struct, the errors, plus a working test. You'll paste it in, run anchor build, and have a working escrow program by tonight.
One more brutally honest thing: the install step is where most hackathon participants quit. They Google one error, fall into a Stack Overflow rabbit hole, and lose a day. You won't, because I'm here. Paste any error you get verbatim, and I'll fix it inside 2 minutes.

Go install. Come back with "environment ready" or with an error. Let's not lose today.








Good. Open your **WSL Ubuntu terminal** (search "Ubuntu" in Start menu). Everything below runs inside that black Linux terminal — never in PowerShell or CMD.

Run each block in order. Don't skip verification. If a verification command fails, **stop and paste me the error** before moving on.

## Step 1 — System prerequisites (2 minutes)

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y build-essential pkg-config libssl-dev libudev-dev curl git
```

It'll ask for your Ubuntu password. Type it (you won't see characters — that's normal). Press Enter.

## Step 2 — Install Rust (3 minutes)

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

When it prompts, choose **option 1** (default install). Then:

```bash
source $HOME/.cargo/env
rustc --version
```

✅ Should print `rustc 1.8x.x`. If yes, continue.

## Step 3 — Install Node 20 via nvm (3 minutes)

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
source ~/.bashrc
nvm install 20
nvm use 20
node --version
npm --version
```

✅ Should print `v20.x.x` and an npm version. If yes, install yarn (Anchor uses it):

```bash
npm install -g yarn
yarn --version
```

## Step 4 — Install Solana CLI (2 minutes)

```bash
sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"
```

When it finishes, it prints a line like:
```
Please update your PATH environment variable to include the Solana programs:
export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
```

**Copy that exact `export PATH=...` line.** Run it now, then add it permanently:

```bash
echo 'export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
solana --version
```

✅ Should print `solana-cli 2.x.x` (or 1.18.x).

## Step 5 — Install Anchor via avm (10–15 minutes — be patient)

```bash
cargo install --git https://github.com/coral-xyz/anchor avm --force
avm install 0.30.1
avm use 0.30.1
anchor --version
```

⚠️ **The first `cargo install` is slow** — it compiles a lot of Rust. You'll see hundreds of `Compiling …` lines scroll by for 5–10 min. **It is not frozen.** Let it finish.

✅ Should print `anchor-cli 0.30.1`.

## Step 6 — Configure Solana for devnet + create your dev wallet

```bash
solana config set --url https://api.devnet.solana.com
solana-keygen new --no-bip39-passphrase
solana airdrop 2
solana balance
```

✅ Should print `2 SOL` (or close to it). **Write down the seed phrase it printed during keygen** — store it in a notepad. This is your devnet dev wallet.

If `solana airdrop 2` fails with rate-limit errors, just run it again, or visit https://faucet.solana.com in your browser and paste the address from `solana address`.

## Step 7 — Install Phantom in your Windows Chrome (60 seconds)

This is in **Windows**, not WSL. Open Chrome on Windows:

1. Go to https://phantom.app → Add to Chrome
2. Click the Phantom icon → "Create New Wallet" → write down the seed phrase
3. Click the gear icon → **Developer Settings** → **Testnet Mode: ON**
4. Top of Phantom → switch network → **Devnet**

This Phantom wallet is **separate** from your WSL `solana-keygen` wallet. That's fine. The dev wallet deploys the program; Phantom is what users (and your demo) sign transactions with.

## Step 8 — Smoke test with a throwaway project (5 minutes)

Back in WSL Ubuntu:

```bash
mkdir -p ~/dev && cd ~/dev
anchor init hello --no-git
cd hello
anchor build
```

⚠️ First `anchor build` takes **5–10 minutes** — compiling solana-program from source. Do not panic.

✅ Success looks like a long compile output ending in `Finished` and no red errors.

If this works, **your environment is bulletproof.** Delete the test project:

```bash
cd ~/dev && rm -rf hello
```

---

## When you're done, reply with one of these

- **"all 8 steps passed"** → I write the RemitSol frontend wiring while you set up the project from my QUICKSTART
- **"step N failed"** + paste the **last 30 lines** of red error output → I fix it in one reply

⏱ Realistic timing: **35–45 minutes total**, of which 20 min is just waiting on compiles. Open YouTube on the side during the long ones. Don't close the terminal — PATH changes can get lost.

Go.

























Good. Environment's done — now the actual work begins. Here's exactly what to do, in order. No detours.

## Step 1 — Wire VS Code to WSL (5 minutes)

In **Windows VS Code**, install one extension:

- Open Extensions panel (Ctrl+Shift+X)
- Search **"WSL"** by Microsoft (publisher: Microsoft, ~30M downloads)
- Click Install

That's the only piece you need. It lets VS Code treat your Ubuntu filesystem as native — terminal, Claude in VS Code, file tree, git, everything runs inside WSL. **Do not** open WSL files via `\\wsl$\...` paths in normal VS Code — that's slow and breaks tooling. Always go through the WSL extension.

## Step 2 — Initialize the RemitSol monorepo (10 minutes)

In your **WSL Ubuntu terminal**:

```bash
cd ~/dev
anchor init remitsol --no-git
cd remitsol
git init
git add .
git commit -m "chore: anchor init"
```

Now create the frontend workspace alongside the program:

```bash
# from inside ~/dev/remitsol
mkdir -p apps
cd apps
npx create-next-app@latest web --typescript --tailwind --app --no-src-dir --import-alias "@/*" --use-npm --eslint --no-turbopack
cd ..
```

When `create-next-app` prompts you, accept the defaults shown by the flags. It'll take ~2 minutes.

Then convert the root into a workspace so the program and the web app live cleanly together:

```bash
# at ~/dev/remitsol root
cat > package.json << 'EOF'
{
  "name": "remitsol",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "web": "npm --workspace apps/web run dev",
    "build:program": "anchor build",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "test:program": "anchor test"
  }
}
EOF
```

Verify the structure:

```bash
ls -la
# You should see: Anchor.toml, Cargo.toml, programs/, apps/, tests/, package.json
```

## Step 3 — Open it in VS Code from WSL

From inside `~/dev/remitsol` in your WSL terminal:

```bash
code .
```

The first time, this auto-installs the VS Code WSL server (~30 seconds). Then VS Code opens with a green **"WSL: Ubuntu"** indicator in the bottom-left corner. **If you don't see that green indicator, stop and tell me — you're editing the wrong filesystem and Claude in VS Code will write code to the wrong place.**

Inside that VS Code window, install these extensions (they install into the WSL remote, not Windows):

- **rust-analyzer** (rust-lang)
- **Even Better TOML** (tamasfe)
- **Tailwind CSS IntelliSense** (Tailwind Labs)
- **ESLint** (Microsoft)

## Step 4 — Push to GitHub now, before you write any code

Public repo from minute one. Judges look at commit history — a repo with 6 days of incremental commits looks ten times more credible than one with a single "final" commit on day 6.

```bash
# inside ~/dev/remitsol
gh auth login   # if you have GitHub CLI; otherwise create the repo on github.com manually
gh repo create remitsol --public --source=. --remote=origin --push
```

If you don't have `gh`, just create an empty repo on github.com called `remitsol`, then:

```bash
git remote add origin https://github.com/<your-username>/remitsol.git
git branch -M main
git push -u origin main
```

## Step 5 — The first real piece of code: the Anchor program

This is where you stop following setup steps and start building the actual product. The program is the foundation — frontend depends on the program's IDL (interface definition), so we write this first.

Open `programs/remitsol/src/lib.rs` in VS Code. Replace the entire file with this minimal but functional escrow program:

```rust
use anchor_lang::prelude::*;
use anchor_spl::token::{self, Token, TokenAccount, Transfer, Mint};

declare_id!("11111111111111111111111111111111"); // we'll replace after first build

#[program]
pub mod remitsol {
    use super::*;

    pub fn create_transfer(
        ctx: Context<CreateTransfer>,
        claim_code_hash: [u8; 32],
        amount: u64,
    ) -> Result<()> {
        let transfer = &mut ctx.accounts.transfer;
        transfer.sender = ctx.accounts.sender.key();
        transfer.claim_code_hash = claim_code_hash;
        transfer.amount = amount;
        transfer.mint = ctx.accounts.mint.key();
        transfer.claimed = false;
        transfer.created_at = Clock::get()?.unix_timestamp;
        transfer.bump = ctx.bumps.transfer;

        let cpi_ctx = CpiContext::new(
            ctx.accounts.token_program.to_account_info(),
            Transfer {
                from: ctx.accounts.sender_token_account.to_account_info(),
                to: ctx.accounts.escrow_token_account.to_account_info(),
                authority: ctx.accounts.sender.to_account_info(),
            },
        );
        token::transfer(cpi_ctx, amount)?;
        Ok(())
    }

    pub fn claim_transfer(
        ctx: Context<ClaimTransfer>,
        claim_code: String,
    ) -> Result<()> {
        let transfer = &mut ctx.accounts.transfer;
        require!(!transfer.claimed, RemitError::AlreadyClaimed);

        let hash = anchor_lang::solana_program::hash::hash(claim_code.as_bytes()).to_bytes();
        require!(hash == transfer.claim_code_hash, RemitError::InvalidCode);

        let sender_key = transfer.sender;
        let seeds = &[b"transfer", sender_key.as_ref(), &transfer.claim_code_hash, &[transfer.bump]];
        let signer = &[&seeds[..]];

        let cpi_ctx = CpiContext::new_with_signer(
            ctx.accounts.token_program.to_account_info(),
            Transfer {
                from: ctx.accounts.escrow_token_account.to_account_info(),
                to: ctx.accounts.recipient_token_account.to_account_info(),
                authority: transfer.to_account_info(),
            },
            signer,
        );
        token::transfer(cpi_ctx, transfer.amount)?;
        transfer.claimed = true;
        Ok(())
    }
}

#[account]
pub struct Transfer {
    pub sender: Pubkey,
    pub claim_code_hash: [u8; 32],
    pub amount: u64,
    pub mint: Pubkey,
    pub claimed: bool,
    pub created_at: i64,
    pub bump: u8,
}

#[derive(Accounts)]
#[instruction(claim_code_hash: [u8; 32])]
pub struct CreateTransfer<'info> {
    #[account(mut)]
    pub sender: Signer<'info>,
    #[account(
        init,
        payer = sender,
        space = 8 + 32 + 32 + 8 + 32 + 1 + 8 + 1,
        seeds = [b"transfer", sender.key().as_ref(), &claim_code_hash],
        bump
    )]
    pub transfer: Account<'info, Transfer>,
    pub mint: Account<'info, Mint>,
    #[account(mut)]
    pub sender_token_account: Account<'info, TokenAccount>,
    #[account(
        init,
        payer = sender,
        token::mint = mint,
        token::authority = transfer,
        seeds = [b"escrow", transfer.key().as_ref()],
        bump
    )]
    pub escrow_token_account: Account<'info, TokenAccount>,
    pub token_program: Program<'info, Token>,
    pub system_program: Program<'info, System>,
    pub rent: Sysvar<'info, Rent>,
}

#[derive(Accounts)]
pub struct ClaimTransfer<'info> {
    #[account(mut)]
    pub recipient: Signer<'info>,
    #[account(mut)]
    pub transfer: Account<'info, Transfer>,
    #[account(
        mut,
        seeds = [b"escrow", transfer.key().as_ref()],
        bump
    )]
    pub escrow_token_account: Account<'info, TokenAccount>,
    #[account(mut)]
    pub recipient_token_account: Account<'info, TokenAccount>,
    pub token_program: Program<'info, Token>,
}

#[error_code]
pub enum RemitError {
    #[msg("Transfer already claimed")]
    AlreadyClaimed,
    #[msg("Invalid claim code")]
    InvalidCode,
}
```

Add the SPL token dependency to `programs/remitsol/Cargo.toml` under `[dependencies]`:

```toml
anchor-spl = "0.30.1"
```

Then build:

```bash
anchor build
```

First build is **5–10 minutes**. After it finishes, get your real program ID:

```bash
solana address -k target/deploy/remitsol-keypair.json
```

Copy that address. Open `programs/remitsol/src/lib.rs` and replace the placeholder in `declare_id!("…")` with that real address. Also update `Anchor.toml` under `[programs.devnet]`. Then:

```bash
anchor build
anchor deploy --provider.cluster devnet
```

If deploy fails with "insufficient funds," run `solana airdrop 2` and retry. You'll need ~3-5 SOL on devnet for deployment — keep airdropping if needed.

## Reply with one of these

- **"program deployed, here's the program ID: <id>"** → I write the full frontend wiring next: wallet adapter setup, the send page, the claim page, and the killer fee-comparison animation
- **"build/deploy failed"** + paste the **last 40 lines** of red error output → I fix it

Realistic timing for everything above: **45-60 min** (most of it is the first Anchor build waiting). One brutally honest reminder: **don't get clever and skip the smoke-test commits to GitHub.** A repo with hourly commits is one of the things that signals "real engineer" to judges browsing 800 submissions.

Go.